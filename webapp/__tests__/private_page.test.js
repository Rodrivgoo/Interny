
test('attempt to access private page', async () => {
    // const response = await fetch('http://localhost:3000/app/[lang]/(private)/dashboard/student/dashboard')
    // expect(response.status).toBe(200)
})
